[project]
name = "dronesimlowfid"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.3.3",
    "pandas>=2.3.2",
    "scipy>=1.16.1",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
